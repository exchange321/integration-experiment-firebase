/**
 * Created by Wayuki on 03-Feb-17 0003.
 */
import initialState from './initialState';

const topicReducer = (state = initialState.topicPage, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
};

export default topicReducer;
